1. Compile:
gcc spoof_syslog.c -o ./spoof
2. Uncompress the sample logs:
gzip -d syslog.sample.gz
3. Run logreplay.pl
./logreplay.pl -f ./syslog.sample -s ./spoof 


Note2 : You can randomize the last octect of IP's by adding a -r to the command line.

run ./logreplay.pl -h for full help.

